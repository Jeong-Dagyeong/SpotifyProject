import { useQuery } from '@tanstack/react-query'

const useGetNewRelease = () => {
  // return useQuery({
  //   queryKey: ['new-release'],
  //   queryFn: async () => {
  //     return useGetNewRelease()
  //   },
  // })
}
